% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_one_euro.R
\name{filter_one_euro}
\alias{filter_one_euro}
\title{Apply the One Euro filter}
\usage{
filter_one_euro(
  x,
  sampling_rate,
  min_cutoff = 1,
  beta = 0,
  d_cutoff = 1,
  na_action = c("linear", "spline", "stine", "locf", "value", "error"),
  keep_na = TRUE,
  ...
)
}
\arguments{
\item{x}{Numeric vector to filter.}

\item{sampling_rate}{Sampling rate of the signal in Hz.}

\item{min_cutoff}{Minimum cutoff frequency in Hz, the cutoff used when
the signal is not moving. Lower means smoother but laggier. Default \code{1}.}

\item{beta}{Speed coefficient. \code{0} gives a plain low-pass filter at
\code{min_cutoff}; larger values raise the cutoff more sharply as the signal
speeds up, cutting lag. Default \code{0}.}

\item{d_cutoff}{Cutoff frequency in Hz for the derivative estimate, which
keeps noise in the derivative from driving the adaptation. Default \code{1}.}

\item{na_action}{Method used to fill \code{NA} values \emph{before} filtering, so
the filter sees a complete series. One of \code{"linear"} (default),
\code{"spline"}, \code{"stine"}, \code{"locf"}, \code{"value"}, or \code{"error"} to abort when
\code{NA}s are present. Filling is internal: whether the filled values reach
the output is controlled by \code{keep_na}.}

\item{keep_na}{Logical. If \code{TRUE} (default), positions that were \code{NA} in
the input are \code{NA} in the output — gaps stay gaps. If \code{FALSE}, the
values used to fill those gaps are kept, so the output has \strong{fewer
\code{NA}s than the input} and genuinely-missing stretches come back as
interpolated estimates.}

\item{...}{Additional arguments passed to \code{\link[=replace_na_with]{replace_na_with()}}.}
}
\value{
Filtered numeric vector, same length as \code{x}.
}
\description{
An adaptive low-pass filter that trades jitter against lag according to
how fast the signal is moving: heavy smoothing while the subject is
nearly still, light smoothing while it moves quickly.
}
\details{
A fixed low-pass filter forces one compromise on the whole recording. Set
the cutoff low and slow passages come out clean but fast ones lag behind;
set it high and fast passages track well but slow ones jitter. The One
Euro filter (Casiez, Roussel & Vogel, 2012) removes the compromise by
making the cutoff a function of the estimated speed:

\deqn{f_c = f_{c_{min}} + \beta |\dot{x}|}

where \eqn{\dot{x}} is itself low-pass filtered, at \code{d_cutoff}, so that
noise in the derivative does not drive the cutoff around.

Tuning, following the authors' advice, is two-stage:
\enumerate{
\item Set \code{beta = 0} and lower \code{min_cutoff} until jitter is acceptable while
the subject is still.
\item Raise \code{beta} until lag is acceptable while it moves quickly.
}

\code{min_cutoff} therefore governs the slow-movement end and \code{beta} the
fast-movement end, and the two can be tuned almost independently.

The filter is recursive, so it needs a complete series: \code{NA}s are filled
by \code{na_action} before filtering. With \code{keep_na = TRUE} (the default) they
are restored afterwards, so the gaps are not silently invented.
}
\examples{
t <- seq(0, 2, by = 1 / 60)
x <- ifelse(t < 1, 0, 10) + rnorm(length(t), 0, 0.1)

# beta = 0 is a plain low-pass: smooth, but slow to follow the step
filter_one_euro(x, sampling_rate = 60, min_cutoff = 0.5)

# raising beta keeps the still passages smooth but tracks the step
filter_one_euro(x, sampling_rate = 60, min_cutoff = 0.5, beta = 0.5)

}
\references{
Casiez, G., Roussel, N., & Vogel, D. (2012). 1 € Filter: A Simple
Speed-based Low-pass Filter for Noisy Input in Interactive Systems.
\emph{Proceedings of the SIGCHI Conference on Human Factors in Computing
Systems (CHI '12)}, 2527–2530. \doi{10.1145/2207676.2208639}
}
\seealso{
\code{\link[=filter_lowpass]{filter_lowpass()}} for a fixed-cutoff Butterworth filter.
}
