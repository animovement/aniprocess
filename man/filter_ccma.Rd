% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_ccma.R
\name{filter_ccma}
\alias{filter_ccma}
\title{Apply Curvature-Corrected Moving Average (CCMA)}
\usage{
filter_ccma(
  data,
  window_width_ma = 11,
  window_width_cc = 7,
  kernel = c("hanning", "uniform"),
  boundary = c("padding"),
  cc_mode = TRUE,
  na_action = c("linear", "spline", "stine", "locf", "value", "error"),
  keep_na = TRUE,
  ...
)
}
\arguments{
\item{data}{A data frame of 2 or 3 numeric coordinate columns. The
curvature math is Cartesian-specific (cross products, Euclidean norms,
circumradius), so coordinates must be Cartesian. For a whole aniframe
use \code{\link[=filter_across]{filter_across()}}, which selects the spatial columns from
\code{variables_where} and rejects non-Cartesian frames.}

\item{window_width_ma}{Integer width of the moving-average kernel
(must be odd; even values are rounded up). Larger = more smoothing.
Default \code{11}.}

\item{window_width_cc}{Integer width of the curvature-correction
kernel (must be odd). Larger = smoother correction but uses
curvature info from further away. Default \code{7}.}

\item{kernel}{Kernel shape for both stages. One of \code{"hanning"}
(default; raised cosine) or \code{"uniform"} (boxcar).}

\item{boundary}{Edge-handling strategy. Currently only \code{"padding"}
(repeat the first and last point so output length equals input
length).}

\item{cc_mode}{If \code{FALSE}, returns just the moving-average result
without curvature correction. Useful for comparison.}

\item{na_action}{Method used to fill \code{NA} values \emph{before} filtering, so
the filter sees a complete series. One of \code{"linear"} (default),
\code{"spline"}, \code{"stine"}, \code{"locf"}, \code{"value"}, or \code{"error"} to abort when
\code{NA}s are present. Filling is internal: whether the filled values reach
the output is controlled by \code{keep_na}.}

\item{keep_na}{Logical. If \code{TRUE} (default), positions that were \code{NA} in
the input are \code{NA} in the output — gaps stay gaps. If \code{FALSE}, the
values used to fill those gaps are kept, so the output has \strong{fewer
\code{NA}s than the input} and genuinely-missing stretches come back as
interpolated estimates.}

\item{...}{Additional arguments passed to \code{\link[=replace_na_with]{replace_na_with()}} (e.g.
\code{value}, \code{min_gap}, \code{max_gap}).}
}
\value{
An aniframe of the same shape as the input, with the
spatial columns smoothed.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Smooths a trajectory while undoing the inward "corner-cutting" bias that
a plain moving average introduces on curved paths.
}
\details{
A plain moving average pulls each point toward the chord between its
neighbours, which lies \emph{inside} the curve — so smoothed circles shrink
inward. CCMA (Steinecker & Wuensche, 2023) estimates how much shrinkage
the moving average caused at each point — from the local curvature and
the kernel — and pushes the result back outward by exactly that amount.

The algorithm has two stages:
\enumerate{
\item \strong{Moving average} of the spatial coordinates with a kernel of width
\code{window_width_ma}.
\item \strong{Curvature correction}: at each output position, sum a kernel of
width \code{window_width_cc} of curvature-derived shifts and apply them
outward in the curve's plane.
}

Because curvature is intrinsically multi-dimensional, this filter
operates on all spatial coordinates jointly (unlike the per-column
filters dispatched through \code{\link[=filter_across]{filter_across()}}). It is most useful
for smoothing curved 2D or 3D trajectories where a plain moving
average visibly cuts corners; for general-purpose time-series
smoothing reach for \code{\link[=filter_gaussian]{filter_gaussian()}} or \code{\link[=filter_sgolay]{filter_sgolay()}}.
}
\section{Input shape}{

This is a \strong{column-level} function: it takes a data frame of coordinate
columns and returns one of the same shape. The aniframe tier is
\code{\link[=filter_across]{filter_across()}}, which applies it within the frame's existing grouping
so each individual / track / keypoint is smoothed as its own trajectory.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{filter_across(af, "ccma")                             # a whole aniframe
data |> mutate(filter_ccma(pick(all_of(c("x", "y")))))  # the columns
}\if{html}{\out{</div>}}

\code{\link[dplyr:pick]{dplyr::pick()}} supplies the columns and the result is spliced back over
them.

CCMA is multivariate — each output coordinate depends on all of them —
so it cannot be used with \code{\link[dplyr:across]{dplyr::across()}}, which passes one column at
a time.
}

\examples{
# The column tier: a data frame of coordinates
coords <- data.frame(x = sin(seq(0, 6, length.out = 40)), y = seq(0, 6, length.out = 40))
head(filter_ccma(coords, window_width_ma = 11, window_width_cc = 7))

# The aniframe tier
af <- anicore::example_aniframe(n_obs = 60, n_individuals = 1, n_keypoints = 1)
filter_across(af, "ccma", window_width_ma = 11, window_width_cc = 7)

}
\references{
Steinecker, T. & Wuensche, H.-J. (2023). A Simple and Model-Free
Path Filtering Algorithm for Smoothing and Accuracy. \emph{2023 IEEE
Intelligent Vehicles Symposium (IV)}.

Reference Python implementation:
\url{https://github.com/UniBwTAS/ccma}
}
