% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_na_across.R
\name{replace_na_across}
\alias{replace_na_across}
\title{Fill missing values across an aniframe's spatial columns}
\usage{
replace_na_across(
  data,
  method = c("linear", "spline", "stine", "locf", "value"),
  variables = NULL,
  ...
)
}
\arguments{
\item{data}{An aniframe.}

\item{method}{Character string specifying the replacement method:
\itemize{
\item \code{"linear"}: Linear interpolation (default)
\item \code{"spline"}: Spline interpolation for smoother curves
\item \code{"stine"}: Stineman interpolation preserving data shape
\item \code{"locf"}: Last observation carried forward
\item \code{"value"}: Replace with a constant value
}}

\item{variables}{Columns to fill, as a tidyselect expression. Defaults
to the \code{variables_where} metadata field.}

\item{...}{Arguments passed to \code{\link[=replace_na_with]{replace_na_with()}}, such as \code{value},
\code{min_gap} and \code{max_gap}.}
}
\value{
An aniframe of the same shape, with gaps filled.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The aniframe-level entry point to the \verb{replace_na_*()} family. Fills
gaps in the columns given by the \code{variables_where} metadata field,
within the frame's existing grouping — so a gap is never filled by
interpolating between two different tracks.
}
\examples{
\dontrun{
replace_na_across(tracking_data, "linear", max_gap = 5)
replace_na_across(tracking_data, "value", value = 0, variables = c(x, y))
}

}
\seealso{
\code{\link[=replace_na_with]{replace_na_with()}} for the vector-level generic.
}
