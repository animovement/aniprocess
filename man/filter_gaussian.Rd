% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_gaussian.R
\name{filter_gaussian}
\alias{filter_gaussian}
\title{Apply Gaussian Kernel Smoother}
\usage{
filter_gaussian(x, sigma = 1, window_width = NULL, keep_na = TRUE)
}
\arguments{
\item{x}{Numeric vector to filter.}

\item{sigma}{Standard deviation of the Gaussian kernel, in samples
(frames). Must be positive.}

\item{window_width}{Integer kernel width in samples. Must be positive
and is forced to be odd. Defaults to \code{2 * ceiling(3 * sigma) + 1},
which truncates the kernel at ±3σ.}

\item{keep_na}{Logical. If \code{TRUE} (default), positions that were \code{NA} in
the input are \code{NA} in the output — gaps stay gaps. If \code{FALSE}, the
values used to fill those gaps are kept, so the output has \strong{fewer
\code{NA}s than the input} and genuinely-missing stretches come back as
interpolated estimates.}
}
\value{
Filtered numeric vector, same length as \code{x}.
}
\description{
Convolves a numeric vector with a discrete Gaussian kernel.
}
\details{
The kernel is symmetric and centered: \code{weights[k] = dnorm(k, sd = sigma)}
for \code{k} in \code{-half:half} (where \code{half = (window_width - 1) / 2}),
renormalised to sum to 1. The output is the kernel-weighted moving
average of \code{x}.

At each position, weights for kernel taps that would fall outside \code{x}
or that align with \code{NA} values are excluded, and the remaining weights
are renormalised. This means edges and isolated \code{NA}s are handled
gracefully without contaminating the result. A position whose entire
window is \code{NA} returns \code{NA}.

Larger \code{sigma} gives heavier smoothing. For movement data, typical
values range from \code{0.5} (very light smoothing) to \code{5} (heavy
smoothing).
}
\examples{
x <- c(1, 2, 3, 100, 5, 6, 7)
filter_gaussian(x, sigma = 1)

}
