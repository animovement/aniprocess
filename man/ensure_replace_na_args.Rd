% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensure.R
\name{ensure_replace_na_args}
\alias{ensure_replace_na_args}
\title{Validate the common arguments shared by \verb{replace_na_*()} functions.}
\usage{
ensure_replace_na_args(x, min_gap, max_gap)
}
\arguments{
\item{x}{The numeric input to validate.}

\item{min_gap, max_gap}{Gap-size thresholds.}
}
\value{
Invisibly \code{NULL}. Called for side effects (errors).
}
\description{
Aborts on a non-numeric \code{x}, \code{min_gap < 1}, or \code{max_gap < min_gap}.
Used at the start of every \verb{replace_na_*()} function so the same
messages and behaviour apply consistently when those functions are
called directly (rather than via \code{\link[=replace_na]{replace_na()}}).
}
\keyword{internal}
