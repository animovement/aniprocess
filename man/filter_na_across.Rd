% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_na_across.R
\name{filter_na_across}
\alias{filter_na_across}
\title{Mask values to NA across an aniframe's spatial columns}
\usage{
filter_na_across(
  data,
  method = c("range", "speed", "excursion", "roi", "confidence"),
  variables = NULL,
  ...
)
}
\arguments{
\item{data}{An aniframe.}

\item{method}{Criterion to apply. One of \code{"range"}, \code{"speed"},
\code{"excursion"}, \code{"roi"} or \code{"confidence"}.}

\item{variables}{Columns to mask, as a tidyselect expression. Defaults
to the \code{variables_where} metadata field.}

\item{...}{Arguments passed to the underlying function.}
}
\value{
An aniframe of the same shape, with failing values replaced by
\code{NA}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The aniframe-level entry point to the \verb{filter_na_*()} family. Applies a
named criterion to the columns given by the \code{variables_where} metadata
field, within the frame's existing grouping — so a criterion is never
evaluated across a track boundary.
}
\details{
Beyond looping over columns, it fills in what the frame already knows:
\code{"speed"} takes its \code{time} from the column named by \code{variables_when},
and \code{"confidence"} takes its \code{confidence} from the column of that name.
Either can be passed explicitly to override.

Only \code{"range"} is univariate. The others decide per row using all the
selected columns at once, and blank every one of them on a flagged row.

Where a \code{confidence} column is present, it is blanked on rows that this
call masked — the cross-column effect that the vector-level functions
cannot perform, since \code{confidence} is not a coordinate.
}
\examples{
\dontrun{
# time comes from the aniframe's metadata
filter_na_across(tracking_data, "speed", threshold = "auto")

filter_na_across(tracking_data, "range", min_value = 0, max_value = 1920)
}

}
\seealso{
\code{\link[=filter_na_with]{filter_na_with()}} for the vector-level generic.
}
