% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/na_handling.R
\name{restore_na}
\alias{restore_na}
\title{Restore NAs to their original positions after filtering}
\usage{
restore_na(result, na_positions, keep_na)
}
\arguments{
\item{result}{Filter output, the same shape as the original input.}

\item{na_positions}{Logical vector or matrix from \code{\link[=prepare_na]{prepare_na()}}, or from
\code{is.na()} on the original input.}

\item{keep_na}{Logical. When \code{FALSE}, \code{result} is returned untouched.}
}
\value{
\code{result}, with \code{NA} written back at \code{na_positions} when
\code{keep_na} is \code{TRUE}.
}
\description{
The counterpart to \code{\link[=prepare_na]{prepare_na()}}, and also usable on its own by filters
that never fill (they infer through gaps, or handle \code{NA} natively) but
should still honour \code{keep_na}.
}
\keyword{internal}
