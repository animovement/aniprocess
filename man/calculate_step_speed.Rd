% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_na_speed.R
\name{calculate_step_speed}
\alias{calculate_step_speed}
\title{Calculate per-row outlier speed from position and time}
\usage{
calculate_step_speed(coords, time)
}
\arguments{
\item{coords}{A data frame of numeric coordinate columns.}

\item{time}{Numeric vector of time values, same length as \code{nrow(coords)}.}
}
\value{
Numeric vector of speed values, of length \code{nrow(coords)}.
}
\description{
Returns the minimum of the backward and forward step speeds at each row.
Endpoints fall back to the one available side; if either side is NA, the
other is used (\code{pmin} with \code{na.rm = TRUE}).
}
\details{
Dimension-agnostic: the Euclidean step is summed over whatever columns
\code{coords} holds, so the same function serves 1D, 2D and 3D data. Intended
to be called inside \code{dplyr::mutate()} with \code{dplyr::pick()}, which supplies
one group's coordinates at a time.
}
\keyword{internal}
