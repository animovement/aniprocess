% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_with.R
\name{filter_with}
\alias{filter_with}
\title{Apply a filter by name}
\usage{
filter_with(
  x,
  method = c("gaussian", "rollmean", "rollmedian", "triangular", "sgolay", "lowpass",
    "highpass", "lowpass_fft", "highpass_fft", "kalman", "kalman_irregular", "ccma"),
  ...
)
}
\arguments{
\item{x}{A numeric vector, or a data frame of numeric coordinate columns.}

\item{method}{Filter to apply. One of \code{"gaussian"}, \code{"rollmean"},
\code{"rollmedian"}, \code{"triangular"}, \code{"sgolay"}, \code{"lowpass"}, \code{"highpass"},
\code{"lowpass_fft"}, \code{"highpass_fft"}, \code{"kalman"}, \code{"kalman_irregular"} or
\code{"ccma"}.}

\item{...}{Arguments passed to the underlying filter.}
}
\value{
The same shape as \code{x}, filtered.
}
\description{
Generic entry point to the \verb{filter_*()} family: pick the method with an
argument rather than by choosing a function. Useful when the method is
itself a parameter — comparing filters, or driving one from a config.
}
\details{
Returns the same shape it is given. A numeric vector gives a numeric
vector; a data frame of coordinate columns gives a data frame.

Most methods are univariate and are applied one column at a time when
given a frame. \code{"ccma"} is multivariate — each output coordinate depends
on all of them — so it requires a frame and cannot be used with
\code{\link[dplyr:across]{dplyr::across()}}.

Method-specific arguments are passed through \code{...}, so a required one
still has to be supplied: \code{sampling_rate} for \code{"sgolay"}, \code{"lowpass"},
\code{"highpass"}, the \verb{_fft} variants and \code{"kalman"}; \code{times} for
\code{"kalman_irregular"}; \code{cutoff_freq} for the frequency filters.
}
\examples{
x <- c(1, 2, 3, 100, 5, 6, 7, 8, 9)

filter_with(x, "gaussian", sigma = 1)
filter_with(x, "rollmean", window_width = 3)

# A frame is filtered column by column
filter_with(data.frame(x = x, y = rev(x)), "rollmean", window_width = 3)

}
\seealso{
\code{\link[=filter_across]{filter_across()}} to apply a filter across an aniframe's
spatial columns.
}
