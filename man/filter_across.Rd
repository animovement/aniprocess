% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_across.R
\name{filter_across}
\alias{filter_across}
\title{Apply a filter across an aniframe's spatial columns}
\usage{
filter_across(
  data,
  method = c("gaussian", "rollmean", "rollmedian", "triangular", "sgolay", "lowpass",
    "highpass", "lowpass_fft", "highpass_fft", "kalman", "kalman_irregular", "ccma"),
  variables = NULL,
  ...,
  use_derivatives = FALSE
)
}
\arguments{
\item{data}{An aniframe.}

\item{method}{Filter to apply. One of \code{"gaussian"}, \code{"rollmean"},
\code{"rollmedian"}, \code{"triangular"}, \code{"sgolay"}, \code{"lowpass"}, \code{"highpass"},
\code{"lowpass_fft"}, \code{"highpass_fft"}, \code{"kalman"}, \code{"kalman_irregular"} or
\code{"ccma"}.}

\item{variables}{Columns to filter, as a tidyselect expression.
Defaults to the \code{variables_where} metadata field.}

\item{...}{Arguments passed to the underlying filter.}

\item{use_derivatives}{If \code{TRUE}, difference each column, filter the
differences, and re-integrate. For trackball data, where the raw
measurements are per-frame displacements and the coordinates were
integrated from them, smoothing belongs on the displacements.}
}
\value{
An aniframe of the same shape, with the selected columns
filtered.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The aniframe-level entry point to the \verb{filter_*()} family. Applies a
named filter to the columns given by the \code{variables_where} metadata
field, within the frame's existing grouping.
}
\details{
This is the aniframe tier of the interface:
\itemize{
\item \code{\link[=filter_gaussian]{filter_gaussian()}} and friends filter one vector — use them inside
\code{\link[dplyr:mutate]{dplyr::mutate()}}.
\item \code{\link[=filter_with]{filter_with()}} does the same but picks the method by name.
\item \code{filter_across()} applies a method to a whole aniframe.
}

Beyond looping over columns, it fills in what the frame already knows:
\code{sampling_rate} comes from metadata for the methods that need it, and
\code{"kalman_irregular"} takes its \code{times} from the column named by
\code{variables_when}. Either can still be passed explicitly to override.

\code{"ccma"} is multivariate — each output coordinate depends on all of
them — so it is applied jointly rather than column by column.
}
\examples{
\dontrun{
# sampling_rate is taken from the aniframe's metadata
filter_across(tracking_data, "lowpass", cutoff_freq = 5)

# restrict to some of the spatial columns
filter_across(tracking_data, "gaussian", variables = c(x, y), sigma = 2)
}

}
\seealso{
\code{\link[=filter_with]{filter_with()}} for the vector-level generic.
}
