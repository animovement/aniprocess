% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_na_with.R
\name{filter_na_with}
\alias{filter_na_with}
\title{Mask values to NA by a named criterion}
\usage{
filter_na_with(
  x,
  method = c("range", "speed", "excursion", "roi", "confidence"),
  ...
)
}
\arguments{
\item{x}{A numeric vector, or a data frame of numeric coordinate columns.}

\item{method}{Criterion to apply. One of \code{"range"}, \code{"speed"},
\code{"excursion"}, \code{"roi"} or \code{"confidence"}.}

\item{...}{Arguments passed to the underlying function.}
}
\value{
The same shape as \code{x}, with failing values replaced by \code{NA}.
}
\description{
Generic entry point to the \verb{filter_na_*()} family: pick the criterion
with an argument rather than by choosing a function. Every method
replaces values that fail its criterion with \code{NA}; none of them fill or
smooth.
}
\details{
Returns the same shape it is given.

Only \code{"range"} is univariate, and it is applied one column at a time
when given a frame. The rest decide per row using all coordinates at
once, so they require a frame of coordinate columns and cannot be used
with \code{\link[dplyr:across]{dplyr::across()}}.

Method-specific arguments go through \code{...}: \code{min_value}/\code{max_value} for
\code{"range"}, \code{threshold} and \code{time} for \code{"speed"}, \code{outlier_sd}/\code{return_sd}
for \code{"excursion"}, the ROI bounds for \code{"roi"}, and \code{threshold} plus
\code{confidence} for \code{"confidence"}.
}
\examples{
filter_na_with(c(1, 5, 10, 15), "range", min_value = 3, max_value = 12)

coords <- data.frame(x = c(0, 1, 2, 50, 4), y = c(0, 0, 0, 0, 0))
filter_na_with(coords, "speed", threshold = 10, time = 1:5)

}
\seealso{
\code{\link[=filter_with]{filter_with()}} for the smoothing and frequency filters,
\code{\link[=replace_na_with]{replace_na_with()}} for filling gaps.
}
