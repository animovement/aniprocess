% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/na_handling.R
\name{prepare_na}
\alias{prepare_na}
\title{Record and fill NA positions before filtering}
\usage{
prepare_na(x, na_action, replace_na_args = list(), call = rlang::caller_env())
}
\arguments{
\item{x}{Numeric vector or matrix.}

\item{na_action}{One of the \code{\link[=replace_na]{replace_na()}} methods, or \code{"error"}.}

\item{replace_na_args}{Named list of extra arguments for \code{\link[=replace_na]{replace_na()}},
typically \code{list(...)} from the calling filter.}

\item{call}{Environment used for the error's call context.}
}
\value{
A list with \code{x} (the filled input) and \code{na_positions} (a logical
vector or matrix, matching the shape of \code{x}, marking the original
\code{NA}s).
}
\description{
Captures where the \code{NA}s are, then fills them via \code{\link[=replace_na]{replace_na()}} so the
filter receives a complete series. Pair with \code{\link[=restore_na]{restore_na()}} to put the
gaps back afterwards.
}
\details{
Accepts a numeric vector or a numeric matrix; a matrix is filled
column-wise, since interpolating down a column is meaningful whereas
interpolating across coordinates is not.
}
\keyword{internal}
