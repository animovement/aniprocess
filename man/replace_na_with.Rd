% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/replace_na_with.R
\name{replace_na_with}
\alias{replace_na_with}
\title{Fill missing values by a named method}
\usage{
replace_na_with(
  x,
  method = c("linear", "spline", "stine", "locf", "value"),
  value = NULL,
  min_gap = 1,
  max_gap = Inf,
  ...
)
}
\arguments{
\item{x}{A numeric vector, or a data frame of numeric columns, with
missing values to fill.}

\item{method}{Character string specifying the replacement method:
\itemize{
\item \code{"linear"}: Linear interpolation (default)
\item \code{"spline"}: Spline interpolation for smoother curves
\item \code{"stine"}: Stineman interpolation preserving data shape
\item \code{"locf"}: Last observation carried forward
\item \code{"value"}: Replace with a constant value
}}

\item{value}{Numeric value for replacement when \code{method = "value"}.}

\item{min_gap}{Integer specifying the minimum gap size to fill. Gaps
shorter than this are left as \code{NA}. Default \code{1} (fill all gaps).}

\item{max_gap}{Integer or \code{Inf} specifying the maximum gap size to fill.
Gaps longer than this are left as \code{NA}. Default \code{Inf} (no limit).}

\item{...}{Additional parameters passed to the underlying function.}
}
\value{
The same shape as \code{x}, with gaps filled where the gap-length
criteria are met.
}
\description{
Generic entry point to the \verb{replace_na_*()} family: pick the method with
an argument rather than by choosing a function.
}
\details{
Returns the same shape it is given. Every method is univariate, so a
data frame is filled one column at a time — meaning this generic does
work with \code{\link[dplyr:across]{dplyr::across()}} as well as \code{\link[dplyr:pick]{dplyr::pick()}}.
}
\examples{
x <- c(1, NA, NA, 4, 5, NA, NA, NA, 9)

replace_na_with(x, "linear")
replace_na_with(x, "value", value = 0)
replace_na_with(x, "linear", max_gap = 2)

# A frame is filled column by column
replace_na_with(data.frame(a = x, b = rev(x)), "locf")

}
\seealso{
\code{\link[=filter_with]{filter_with()}} for smoothing, \code{\link[=filter_na_with]{filter_na_with()}} for masking.
\code{\link[=replace_na]{replace_na()}} is the original name of this function and still works.
}
