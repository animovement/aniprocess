% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_aniframe.R
\name{filter_aniframe}
\alias{filter_aniframe}
\title{Smooth Movement Data}
\usage{
filter_aniframe(
  data,
  method = c("rollmedian", "rollmean", "kalman", "sgolay", "lowpass", "highpass",
    "lowpass_fft", "highpass_fft"),
  use_derivatives = FALSE,
  ...
)
}
\arguments{
\item{data}{An aniframe. Spatial columns to filter are taken from the
metadata field \code{variables_where} (e.g. \code{c("x", "y")} or \code{c("x", "y", "z")}).
Filtering is applied within the aniframe's existing grouping, which is
driven by \code{variables_what} (e.g. \code{c("individual", "keypoint")}, \code{"track"},
or \code{character(0)} for a single trajectory). Single-track data without an
\code{individual} column is supported.}

\item{method}{Character string specifying the smoothing method. Options:
\itemize{
\item \code{"kalman"}: Kalman filter (see \code{\link[=filter_kalman]{filter_kalman()}})
\item \code{"sgolay"}: Savitzky-Golay filter (see \code{\link[=filter_sgolay]{filter_sgolay()}})
\item \code{"lowpass"}: Low-pass filter (see \code{\link[=filter_lowpass]{filter_lowpass()}})
\item \code{"highpass"}: High-pass filter (see \code{\link[=filter_highpass]{filter_highpass()}})
\item \code{"lowpass_fft"}: FFT-based low-pass filter (see \code{\link[=filter_lowpass_fft]{filter_lowpass_fft()}})
\item \code{"highpass_fft"}: FFT-based high-pass filter (see \code{\link[=filter_highpass_fft]{filter_highpass_fft()}})
\item \code{"rollmean"}: Rolling mean filter (see \code{\link[=filter_rollmean]{filter_rollmean()}})
\item \code{"rollmedian"}: Rolling median filter (see \code{\link[=filter_rollmedian]{filter_rollmedian()}})
}}

\item{use_derivatives}{Filter on the derivative values instead of coordinates
(important for e.g. trackball or accelerometer data)}

\item{...}{Additional arguments passed to the specific filter function}
}
\value{
An aniframe with the same structure as the input, but with smoothed
spatial coordinates.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Applies smoothing filters to movement tracking data to reduce noise.
}
\details{
This function is a wrapper that applies the chosen filter to every spatial
coordinate listed in \code{variables_where}, respecting the aniframe's existing
grouping. Each filtering method has its own specific parameters - see the
documentation of the individual filter functions for details:
\itemize{
\item \code{\link[=filter_kalman]{filter_kalman()}}: Kalman filter parameters
\item \code{\link[=filter_sgolay]{filter_sgolay()}}: Savitzky-Golay filter parameters
\item \code{\link[=filter_lowpass]{filter_lowpass()}}: Low-pass filter parameters
\item \code{\link[=filter_highpass]{filter_highpass()}}: High-pass filter parameters
\item \code{\link[=filter_lowpass_fft]{filter_lowpass_fft()}}: FFT-based low-pass filter parameters
\item \code{\link[=filter_highpass_fft]{filter_highpass_fft()}}: FFT-based high-pass filter parameters
\item \code{\link[=filter_rollmean]{filter_rollmean()}}: Rolling mean parameters (window_width, min_obs)
\item \code{\link[=filter_rollmedian]{filter_rollmedian()}}: Rolling median parameters (window_width, min_obs)
}
}
\examples{
\dontrun{
# Apply rolling median with window of 5
filter_aniframe(tracking_data, "rollmedian", window_width = 5, min_obs = 1)
}

}
\seealso{
\itemize{
\item \code{\link[=filter_kalman]{filter_kalman()}}
\item \code{\link[=filter_sgolay]{filter_sgolay()}}
\item \code{\link[=filter_lowpass]{filter_lowpass()}}
\item \code{\link[=filter_highpass]{filter_highpass()}}
\item \code{\link[=filter_lowpass_fft]{filter_lowpass_fft()}}
\item \code{\link[=filter_highpass_fft]{filter_highpass_fft()}}
\item \code{\link[=filter_rollmean]{filter_rollmean()}}
\item \code{\link[=filter_rollmedian]{filter_rollmedian()}}
}
}
