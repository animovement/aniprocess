% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{dispatch_method}
\alias{dispatch_method}
\title{Dispatch a method name to its implementation.}
\usage{
dispatch_method(
  x,
  method,
  fn,
  multivariate,
  generic,
  ...,
  call = rlang::caller_env()
)
}
\arguments{
\item{x}{A numeric vector or a data frame of coordinate columns.}

\item{method}{The resolved method name.}

\item{fn}{The function implementing \code{method}.}

\item{multivariate}{\code{TRUE} if \code{method} needs all coordinates at once.}

\item{generic}{Name of the calling generic, for error messages.}

\item{...}{Passed on to \code{fn}.}

\item{call}{Environment used for the error's call context.}
}
\value{
The same shape as \code{x}.
}
\description{
Shared machinery for the \verb{*_with()} generics. Applies the
shape-preserving contract: a vector in gives a vector out, a frame of
coordinate columns gives a frame out.
}
\details{
Univariate methods are applied column-wise when given a frame.
Multivariate methods — where each result depends on all coordinates
jointly — require a frame, and say so when handed a bare vector.
}
\keyword{internal}
